name: "Run E2E Tests"
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  merge_group:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: NickCao/jumpstarter-e2e
        with:
          controller-ref: main
          jumpstarter-ref: ${{ github.ref }}
