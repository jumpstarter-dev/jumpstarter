// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.dutlink_serial.v1;

import "google/protobuf/empty.proto";

// DutlinkSerial(*, uuid: uuid.UUID = <factory>, labels: dict[str, str] = <factory>, children: 'dict[str, Driver]' = <factory>, description: 'str | None' = None, methods_description: 'dict[str, str]' = <factory>, log_level: 'str' = 'INFO', serial: 'str | None' = None, timeout_s: 'int' = 20, storage_timeout: 'int' = 10, storage_leeway: 'int' = 6, storage_fsync_timeout: 'int' = 900, baudrate: int = 115200, check_present: bool = True, cps: Optional[float] = None, disable_hupcl: bool = False)
service DutlinkSerial {

  // Force-close any active serial connection by closing the underlying transport.
  //
  // The asyncio stream reader/writer will naturally receive errors or EOF
  // when the fd is closed, causing the stream to tear down without needing
  // explicit signalling.
  //
  // Safe to call when no stream is active (no-op).
  rpc Close(google.protobuf.Empty) returns (google.protobuf.Empty);

  rpc Connect(stream StreamData) returns (stream StreamData);

  // Set the DTR control signal.
  rpc SetDtr(SetDtrRequest) returns (google.protobuf.Empty);

  // Set the RTS control signal.
  rpc SetRts(SetRtsRequest) returns (google.protobuf.Empty);
}

// Byte payload for bidirectional stream methods (@exportstream).
message StreamData {
  bytes payload = 1;
}

message SetDtrRequest {
  bool value = 1;
}

message SetRtsRequest {
  bool value = 1;
}
