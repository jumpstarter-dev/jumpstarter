// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.ble_write_notify_stream.v1;

import "google/protobuf/empty.proto";

// Bluetooth Low Energy (BLE) driver for Jumpstarter
// This driver connects to the specified BLE device and
// provides a write (write_char_uuid) and a read (notify_char_uuid) stream
// for data transfer.
service BleWriteNotifyStream {
  rpc Connect(stream StreamData) returns (stream StreamData);

  rpc Info(google.protobuf.Empty) returns (InfoResponse);
}

// Byte payload for bidirectional stream methods (@exportstream).
message StreamData {
  bytes payload = 1;
}

message InfoResponse {
  string value = 1;
}
