// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.http_server.v1;

import "google/protobuf/empty.proto";

// HTTP Server driver for Jumpstarter
service HttpServer {

  // Get the host IP address of the HTTP server.
  //
  // Returns:
  //     str: Host IP address.
  rpc GetHost(google.protobuf.Empty) returns (GetHostResponse);

  // Get the port number of the HTTP server.
  //
  // Returns:
  //     int: Port number.
  rpc GetPort(google.protobuf.Empty) returns (GetPortResponse);

  // Get the base URL of the HTTP server.
  //
  // Returns:
  //     str: Base URL of the HTTP server.
  rpc GetUrl(google.protobuf.Empty) returns (GetUrlResponse);

  // Start the HTTP server.
  //
  // Raises:
  //     HttpServerError: If the server fails to start.
  rpc Start(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Stop the HTTP server.
  //
  // Raises:
  //     HttpServerError: If the server fails to stop.
  rpc Stop(google.protobuf.Empty) returns (google.protobuf.Empty);
}

message GetHostResponse {
  string value = 1;
}

message GetPortResponse {
  int64 value = 1;
}

message GetUrlResponse {
  string value = 1;
}
