[project]
name = "jumpstarter-cli"
dynamic = ["version", "urls"]
description = ""
authors = [
  { name = "Nick Cao", email = "ncao@redhat.com" },
  { name = "Miguel Angel Ajo Pelayo", email = "majopela@redhat.com" },
  { name = "Kirk Brauer", email = "kbrauer@hatci.com" },
]
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
dependencies = [
    "jumpstarter",
    "jumpstarter-cli-common",
    "jumpstarter-client-cli",
    "jumpstarter-admin-cli",
    "jumpstarter-exporter-cli"
]

[dependency-groups]
dev = [
    "pytest>=8.3.2",
    "pytest-anyio>=0.0.0",
    "pytest-asyncio>=0.0.0",
    "pytest-cov>=5.0.0",
]

[project.scripts]
jmp = "jumpstarter_cli:jmp"

[tool.uv.sources]
jumpstarter-cli-common = { workspace = true }
jumpstarter-admin-cli = { workspace = true }
jumpstarter-client-cli = { workspace = true }
jumpstarter-exporter-cli = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["jumpstarter_cli"]

[tool.hatch.metadata.hooks.vcs.urls]
Homepage = "https://jumpstarter.dev"
source_archive = "https://github.com/jumpstarter-dev/repo/archive/{commit_hash}.zip"

[tool.hatch.version]
source = "vcs"
raw-options = { 'root' = '../../'}

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"
