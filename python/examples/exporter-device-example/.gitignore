# Device-wrapper build artifacts, regenerated by the jumpstarter_codegen build hook from the
# committed exporter.yaml (the source of truth) — never committed.
jumpstarter_exporter_device_example/_generated/
