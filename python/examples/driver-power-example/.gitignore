# Proto-first build artifacts, regenerated by the jumpstarter_codegen build hook from the
# committed interfaces/proto power.proto (the source of truth) — never committed.
jumpstarter_driver_power_example/_generated/
