// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.u_streamer.v1;

import "google/protobuf/empty.proto";

// UStreamer(*, uuid: uuid.UUID = <factory>, labels: dict[str, str] = <factory>, children: 'dict[str, Driver]' = <factory>, description: 'str | None' = None, methods_description: 'dict[str, str]' = <factory>, log_level: 'str' = 'INFO')
service UStreamer {
  rpc Connect(stream StreamData) returns (stream StreamData);

  rpc Snapshot(google.protobuf.Empty) returns (SnapshotResponse);

  rpc State(google.protobuf.Empty) returns (UStreamerState);
}

// Byte payload for bidirectional stream methods (@exportstream).
message StreamData {
  bytes payload = 1;
}

message SnapshotResponse {
  string value = 1;
}

message UStreamerState {
  message Result {
    message Encoder {
      string type = 1;
      int64 quality = 2;
    }
    message Source {
      message Resolution {
        int64 width = 1;
        int64 height = 2;
      }
      bool online = 1;
      int64 desired_fps = 2;
      int64 captured_fps = 3;
      Resolution resolution = 4;
    }
    Encoder encoder = 1;
    Source source = 2;
  }
  bool ok = 1;
  Result result = 2;
}
