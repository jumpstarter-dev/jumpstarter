apiVersion: jumpstarter.dev/v1alpha1
kind: ExporterConfig
metadata:
  namespace: local
  name: demo
endpoint: grpc.jumpstarter.192.168.0.203.nip.io:8082
token: "<token>"
export:
  storage:
    type: jumpstarter_driver_sdmux.driver.UsbSdMux
    config:
      serial: "000000000042"

