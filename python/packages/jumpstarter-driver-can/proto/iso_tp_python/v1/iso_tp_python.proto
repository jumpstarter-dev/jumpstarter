// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.iso_tp_python.v1;

import "google/protobuf/empty.proto";
import "google/protobuf/struct.proto";

// Pure python ISO-TP socket driver
//
// Available on any platform, moderate performance and reliability,
// wide support for non-standard hardware interfaces
service IsoTpPython {

  // Returns `True` if an ISO-TP frame is awaiting in the reception queue, `False` otherwise.
  rpc Available(google.protobuf.Empty) returns (AvailableResponse);

  // Dequeue an ISO-TP frame from the reception queue if available.
  rpc Recv(RecvRequest) returns (IsoTpMessage);

  // Enqueue an ISO-TP frame to send over the CAN network.
  rpc Send(SendRequest) returns (google.protobuf.Empty);

  // Sets the layer address. Can be set after initialization if needed.
  // May cause a timeout if called while a transmission is active.
  rpc SetAddress(SetAddressRequest) returns (google.protobuf.Empty);

  // Start listening for messages.
  rpc Start(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Stop listening for messages.
  rpc Stop(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Stop receiving messages.
  rpc StopReceiving(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Stop sending messages.
  rpc StopSending(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Returns `True` if an ISO-TP frame is being transmitted, `False` otherwise.
  rpc Transmitting(google.protobuf.Empty) returns (TransmittingResponse);
}

message AvailableResponse {
  bool value = 1;
}

message RecvRequest {
  bool block = 1;
  optional double timeout = 2;
}

// An ISO-TP CAN message.
message IsoTpMessage {
  optional string data = 1;
}

message SendRequest {
  message IsoTpMessage {
    optional string data = 1;
  }
  IsoTpMessage msg = 1;
  optional int64 target_address_type = 2;
  optional double send_timeout = 3;
}

message SetAddressRequest {
  google.protobuf.Value address = 1;
}

message TransmittingResponse {
  bool value = 1;
}
