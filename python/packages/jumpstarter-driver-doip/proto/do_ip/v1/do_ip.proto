// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.do_ip.v1;

import "google/protobuf/empty.proto";
import "google/protobuf/struct.proto";

// Raw DoIP (Diagnostics over Internet Protocol) driver.
//
// Provides low-level DoIP operations such as vehicle discovery,
// entity status checks, alive checks, and raw diagnostic message
// exchange per ISO-13400.
service DoIP {

  // Request an alive check from the DoIP entity.
  rpc AliveCheck(google.protobuf.Empty) returns (AliveCheckResponse);

  // Close the DoIP connection.
  rpc CloseConnection(google.protobuf.Empty) returns (google.protobuf.Empty);

  // Request the diagnostic power mode.
  rpc DiagnosticPowerMode(google.protobuf.Empty) returns (DiagnosticPowerModeResponse);

  // Request the DoIP entity status.
  rpc EntityStatus(google.protobuf.Empty) returns (EntityStatusResponse);

  // Receive a raw diagnostic response from the ECU.
  rpc ReceiveDiagnostic(ReceiveDiagnosticRequest) returns (ReceiveDiagnosticResponse);

  // Reconnect to the DoIP entity (e.g. after ECU reset).
  rpc Reconnect(ReconnectRequest) returns (google.protobuf.Empty);

  // Request vehicle identification, optionally filtered by VIN or EID.
  rpc RequestVehicleIdentification(RequestVehicleIdentificationRequest) returns (RequestVehicleIdentificationResponse);

  // Request routing activation for the given activation type.
  rpc RoutingActivation(RoutingActivationRequest) returns (RoutingActivationResponse);

  // Send a raw diagnostic payload to the ECU.
  rpc SendDiagnostic(SendDiagnosticRequest) returns (google.protobuf.Empty);
}

message AliveCheckResponse {
  google.protobuf.Value value = 1;
}

message DiagnosticPowerModeResponse {
  google.protobuf.Value value = 1;
}

message EntityStatusResponse {
  google.protobuf.Value value = 1;
}

message ReceiveDiagnosticRequest {
  double timeout = 1;
}

message ReceiveDiagnosticResponse {
  google.protobuf.Value value = 1;
}

message ReconnectRequest {
  double close_delay = 1;
}

message RequestVehicleIdentificationRequest {
  optional string vin = 1;
  optional string eid = 2;
}

message RequestVehicleIdentificationResponse {
  google.protobuf.Value value = 1;
}

message RoutingActivationRequest {
  int64 activation_type = 1;
}

message RoutingActivationResponse {
  google.protobuf.Value value = 1;
}

message SendDiagnosticRequest {
  google.protobuf.Value payload = 1;
}
