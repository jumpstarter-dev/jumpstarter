// Code generated by jmp interface generate. DO NOT EDIT.

syntax = "proto3";
package jumpstarter.interfaces.shell.v1;

import "google/protobuf/empty.proto";
import "google/protobuf/struct.proto";

// shell driver for Jumpstarter
service Shell {

  // Execute a shell method with live streaming output.
  // Yields (stdout_chunk, stderr_chunk, returncode) tuples.
  // returncode is None until the process completes, then it's the final return code.
  rpc CallMethod(CallMethodRequest) returns (stream CallMethodResponse);

  rpc GetMethods(google.protobuf.Empty) returns (GetMethodsResponse);
}

message CallMethodRequest {
  string method = 1;
  google.protobuf.Value env = 2;
  string args = 3;
}

message CallMethodResponse {
  repeated google.protobuf.Value value = 1;
}

message GetMethodsResponse {
  repeated string value = 1;
}
