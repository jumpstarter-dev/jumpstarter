[package]
name = "jumpstarter-testing"
description = "Jumpstarter testing utilities for Rust — session setup and device wrapper helpers"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
jumpstarter-client = { workspace = true }
jumpstarter-testing-macros = { workspace = true }
tokio = { workspace = true }

# Re-export the proc macro for convenience
[features]
default = []
