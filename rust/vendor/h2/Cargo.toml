# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.63"
name = "h2"
version = "0.4.15"
authors = [
    "Carl Lerche <me@carllerche.com>",
    "Sean McArthur <sean@seanmonstar.com>",
]
build = false
exclude = [
    "fixtures/**",
    "ci/**",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "An HTTP/2 client and server"
documentation = "https://docs.rs/h2"
readme = "README.md"
keywords = [
    "http",
    "async",
    "non-blocking",
]
categories = [
    "asynchronous",
    "web-programming",
    "network-programming",
]
license = "MIT"
repository = "https://github.com/hyperium/h2"

[package.metadata.docs.rs]
features = ["stream"]

[features]
stream = []
unstable = []

[lib]
name = "h2"
path = "src/lib.rs"

[dependencies.atomic-waker]
version = "1.0.0"

[dependencies.bytes]
version = "1"

[dependencies.fnv]
version = "1.0.5"

[dependencies.futures-core]
version = "0.3"
default-features = false

[dependencies.futures-sink]
version = "0.3"
default-features = false

[dependencies.http]
version = "1.1"

[dependencies.indexmap]
version = "2"
features = ["std"]

[dependencies.slab]
version = "0.4.2"

[dependencies.tokio]
version = "1"
features = ["io-util"]

[dependencies.tokio-util]
version = "0.7.1"
features = [
    "codec",
    "io",
]

[dependencies.tracing]
version = "0.1.35"
features = ["std"]
default-features = false

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = ["cfg(fuzzing)"]

# JUMPSTARTER: examples/benches/dev-dependencies removed from this vendored copy —
# it is consumed as a library only, and h2's dev-deps would otherwise perturb the
# workspace lockfile/feature graph (e.g. rustls backend unification).
