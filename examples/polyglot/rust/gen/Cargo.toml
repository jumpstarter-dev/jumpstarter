[package]
name = "jumpstarter-example-board"
version.workspace = true
edition.workspace = true

# This file overrides the codegen-generated Cargo.toml with workspace
# references for use inside the jumpstarter monorepo.
# For standalone use, run `jmp codegen --language rust` to generate a
# Cargo.toml with concrete crates.io version specs.

[dependencies]
jumpstarter-client = { workspace = true }
jumpstarter-testing = { workspace = true }
tonic = { workspace = true }
prost = { workspace = true }
tokio = { workspace = true }
tower = { workspace = true }
bytes = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }
