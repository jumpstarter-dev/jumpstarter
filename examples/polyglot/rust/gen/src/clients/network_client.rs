// Auto-generated by `jmp codegen --language rust`. Do not edit.
// Note: NetworkInterface.Connect is a bidi streaming (@exportstream) method
// that requires RouterService.Stream — not a direct gRPC call.

use jumpstarter_client::ExporterSession;

/// Bidirectional byte stream connection to a network endpoint.
///
/// The Connect method is an @exportstream method that uses RouterService.Stream
/// for raw byte transport. Full implementation requires the StreamChannel runtime.
pub struct NetworkClient {
    _uuid: String,
}

impl NetworkClient {
    /// Create a new client for the named driver instance.
    pub fn new(session: &ExporterSession, driver_name: &str) -> Self {
        let uuid = session.report().find_by_name(driver_name)
            .unwrap_or_else(|| panic!("driver '{}' not found in device tree", driver_name))
            .uuid()
            .to_owned();
        Self { _uuid: uuid }
    }
}
