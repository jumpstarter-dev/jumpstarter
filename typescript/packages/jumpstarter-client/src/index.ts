/**
 * @jumpstarter/client — Jumpstarter client runtime for TypeScript.
 *
 * Provides session management, driver discovery, UUID metadata interception,
 * and stream channel support for typed device wrappers generated by `jmp codegen`.
 *
 * Designed for use under `jmp shell`, which handles lease acquisition,
 * authentication, and channel setup. The runtime reads `JUMPSTARTER_HOST`
 * and creates an insecure gRPC channel to the exporter.
 *
 * @packageDocumentation
 */

export { ExporterSession } from "./ExporterSession";
export { DriverReport } from "./DriverReport";
export type { DriverInstance } from "./DriverReport";
export { StreamChannel } from "./StreamChannel";
export { createUuidInterceptor, uuidInterceptors } from "./UuidMetadataInterceptor";
export { TcpPortforwardAdapter } from "./TcpPortforwardAdapter";
export { UdpPortforwardAdapter } from "./UdpPortforwardAdapter";
